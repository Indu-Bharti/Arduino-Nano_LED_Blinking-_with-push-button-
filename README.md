# Arduino-Nano_LED_Blinking-_with-push-button-
This Project demonstrate the basic concept of digital l/O using Arduino-Nano.     A push button  is used as the input device while an LED acts as the output  device  whenever  the push button  is pressed  the Arduino-Nano detects the input  signal and turn on the LED when button  is realease button LED turn off.                
